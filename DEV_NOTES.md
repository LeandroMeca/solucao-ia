Quick dev notes

- Use the VSCode snippet `rcomp` (React TSX Component) to create a new component scaffold.
- To auto-fix lint/format issues across `src`, run:

```
pnpm run fix
```

-- Make sure `Format on Save` is enabled in VSCode to auto-apply formatting. ESLint was removed from the project.
